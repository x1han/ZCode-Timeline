{
  "name": "zcode-timeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Session timeline sidebar for ZCode, injected via CDP. Upgrade-safe and reversible.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node timeline-src/build.mjs",
    "build:dev": "node timeline-src/build.mjs --dev",
    "start": "node launcher/launcher.mjs",
    "start:clean": "node launcher/start-zcode-clean.mjs",
    "dev": "node scripts/hot-reload.mjs",
    "probe": "node scripts/probe-dom.mjs",
    "verify": "node scripts/verify-patch.mjs",
    "clean": "node -e \"import('node:fs').then(fs => fs.rmSync('dist', { recursive: true, force: true }))\""
  },
  "dependencies": {
    "@electron/asar": "^4.2.0",
    "chrome-remote-interface": "^0.33.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "esbuild": "^0.24.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
